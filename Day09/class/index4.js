class NewMath {
  static add(x, y) {
    return x + y;
  }
  static minus(x, y) {
    return x - y;
  }
  static double(x) {
    return x * 2;
  }
}

const a = NewMath.add(10, 20);
const b = NewMath.minus(20, 10);
const c = NewMath.double(3);

// 클래스

// new Date(), new Regex()
// Math.round()

// new Promise()
// Promise.all()
